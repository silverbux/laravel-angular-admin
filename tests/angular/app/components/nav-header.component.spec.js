ngDescribe({
    name: 'Test nav-header component',
    modules: 'app',
    element: '<nav-header></nav-header>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
