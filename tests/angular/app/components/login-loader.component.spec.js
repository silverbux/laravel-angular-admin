ngDescribe({
    name: 'Test login-loader component',
    modules: 'app',
    element: '<login-loader></login-loader>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
