ngDescribe({
  name: 'Test user-permissions component',
  modules: 'app',
  element: '<user-permissions></user-permissions>',
  tests: function (deps) {
    it('basic test', () => {
      //
    })
  }
})
